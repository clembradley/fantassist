!!!
%html
  %head
    %title Fantassist
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    .navbar.navbar-inverse.navbar-fixed-top{role: "navigation"}
      .container-fluid
        .navbar-header
          %button.navbar-toggle{type: "button", "data-toggle" => "collapse", "data-target" => ".navbar-collapse"}
            %span.src-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{href: "#"} Fantassist
        .navbar-collapse.collapse
          %ul.nav.navbar-nav
            %li.active
              %a{href: "#"} Draft
            %li
              = image_tag('ajax-loader.gif', id: 'ajax-loader', style: 'display: block; padding: 15px; visibility: hidden')
          %ul.nav.navbar-nav.navbar-right
            %li
              - if !current_user
                = link_to "Sign in with Google", user_omniauth_authorize_path(:google_oauth2)
              - else
                = image_tag current_user.image, title: "#{current_user.first_name} #{current_user.last_name}"
                %li
                  = link_to "Log out", destroy_user_session_path
    .container-fluid
      - flash.each do |key, value|
        = content_tag :p, value, class: "flash #{key}"
      = yield
